interface ThemeStyle {
    background: string,
    cardBackground: string,
    sideBar: string,
    text: string,
}

const THEMES = {
    DARK: 'dark',
    LIGHT: 'light'
}

export type { THEMES, ThemeStyle };